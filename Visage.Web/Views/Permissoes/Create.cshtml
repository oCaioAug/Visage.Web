@model Visage.Web.Models.Permisso

@{
    ViewData["Title"] = "Criar Permissão";
}

<h1>@ViewData["Title"]</h1>

<h4>Permissão</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomePermissao" class="control-label"></label>
                <input asp-for="NomePermissao" class="form-control" />
                <span asp-validation-for="NomePermissao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
